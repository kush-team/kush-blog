import { CompileDirective } from './compile.directive';

describe('CompileDirective', () => {
  it('should create an instance', () => {
    const directive = new CompileDirective();
    expect(directive).toBeTruthy();
  });
});
